# Folder src berisi:
# - train.py: pipeline ML sederhana (data prep, training, evaluasi, logging MLflow)
# - dashboard.py: dashboard Streamlit

# Untuk menjalankan pipeline:
# docker-compose up --build
# lalu exec ke container dan jalankan python src/train.py

# Untuk menjalankan dashboard:
# docker-compose up --build
# akses http://localhost:8501
